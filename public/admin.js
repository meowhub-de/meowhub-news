const f=document.querySelector('#news-form'),l=document.querySelector('#admin-news'),s=document.querySelector('#status'),e=x=>String(x??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));async function refresh(){const r=await fetch('/api/news');if(!r.ok)return location.href='/auth/login';const ns=await r.json();l.innerHTML=ns.map(n=>`<div class="item"><div><strong>${e(n.title)}</strong><small>${new Date(n.publishedAt).toLocaleString('de-DE')}</small></div><button class="danger" data-id="${n.id}">Löschen</button></div>`).join('')||'<p>Keine Einträge.</p>';l.querySelectorAll('[data-id]').forEach(b=>b.onclick=async()=>{await fetch(`/api/admin/news/${b.dataset.id}`,{method:'DELETE'});refresh()})}f.onsubmit=async x=>{x.preventDefault();s.textContent='Speichere …';const r=await fetch('/api/admin/news',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(f)))});s.textContent=r.ok?'Veröffentlicht.':'Fehler beim Speichern.';if(r.ok){f.reset();refresh()}};refresh();
