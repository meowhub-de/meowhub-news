const e=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));fetch('/api/news').then(r=>r.json()).then(ns=>document.querySelector('#news').innerHTML=ns.length?ns.map(n=>`<article><time>${new Date(n.publishedAt).toLocaleDateString('de-DE')}</time><h2>${e(n.title)}</h2><p>${e(n.summary||n.content)}</p>${n.url?`<a href="${e(n.url)}" target="_blank" rel="noopener">Weiterlesen →</a>`:''}</article>`).join(''):'<p>Noch keine News veröffentlicht.</p>').catch(()=>document.querySelector('#news').textContent='News konnten nicht geladen werden.');
